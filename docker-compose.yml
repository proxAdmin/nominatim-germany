services:
  nominatim:
    build:
      context: .
      dockerfile: Dockerfile.nominatim
    container_name: nominatim
    ports:
      - "8080:8080"
    volumes:
      - ./nominatim_data:/nominatim/data
    stdin_open: true
    tty: true
    restart: unless-stopped
